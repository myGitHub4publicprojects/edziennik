Dziś jest {{ dzisiejsza_data }}
<h2><a href="{% url 'edziennik:name_sprawdz' %}">Sprawdź obecność</a></h2>
{% if lektor_list %}
    <p>Lektorzy:<p>
    <ul>
    {% for lektor in lektor_list %}
        <li><a href="{% url 'edziennik:name_lektor' lektor.id %}">{{ lektor.lektor_name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No lektors are available.</p>
{% endif %}

{% if group_list %}
    <p>Grupy:<p>
    <ul>
    {% for grupa in group_list %}
        <li><a href="{% url 'edziennik:name_group' grupa.id %}">{{ grupa.group_name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No groups are available.</p>
{% endif %}

{% if group_list %}
    <p>Studenci:<p>
    <ul>
    {% for student in student_list %}
        <li><a href="/edziennik/{{ student.id }}/student">{{ student.student_name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No students are available.</p>
{% endif %}
